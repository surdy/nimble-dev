# Global user-defined environment variables.
# Every script run by dynamic_list or script_action receives these.
# Keys must not start with NIMBLE_ (reserved for built-in variables).
WORK_EMAIL: alice@example.com
TEAM: engineering
