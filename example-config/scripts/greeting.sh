#!/bin/sh
# Shared script referenced via ${NIMBLE_CONFIG_DIR}/scripts/greeting.sh
echo '[{"title":"Hello from a shared script!","subtext":"This script lives in the scripts/ directory, referenced via ${NIMBLE_CONFIG_DIR}"}]'
